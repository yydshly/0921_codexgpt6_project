# Project collaboration

This repository contains the Mid-Autumn product exploration archive and the active moon-studio interactive story editor. Read moon-studio/AGENTS.md for product decisions; its older sections record feedback chronologically, not simultaneous approved directions.

The user requested a public repository named 0921_codexgpt6_project under yydshly and remote deployment. Use the configured origin and GitHub Pages workflow; no extra approval is needed for ordinary updates within this request. Never include credentials, personal browser storage, dependencies or generated caches.

The project overview is the default route (#overview). Preserve all historical routes and their explicit experimental labels. Do not equate technical completion with proven emotional impact or conversion. Maintain README.md, docs/PRODUCT_UNDERSTANDING.md and docs/ASSET_CATALOG.md when the actual scope changes.

Canonical film outputs live in midautumn-film; scripts/prepare-showcase.mjs produces public web copies. Use npm run setup, npm test, npm run examples and npm run build. GitHub Pages uses /0921_codexgpt6_project/; keep canonical project asset paths portable and resolve browser requests through assetUrl. Preserve existing Sites packaging files.
